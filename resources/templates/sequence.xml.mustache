<?xml version="1.0" encoding="UTF-8"?>
<!--
  {{NAME}} — Generated WSO2 MI Sequence
  {{DESCRIPTION}}
-->
<sequence name="{{NAME}}" xmlns="http://ws.apache.org/ns/synapse"
          onError="{{ON_ERROR}}">

    <!-- Step 1: Log entry -->
    <log level="custom" category="INFO">
        <property name="ACTION"  value="[{{NAME}}] Sequence invoked"/>
        <property name="PAYLOAD" expression="json-eval($)"/>
    </log>

    <!-- Step 2: Set content type -->
    <property name="messageType" value="application/json" scope="axis2"/>
    <property name="ContentType" value="application/json" scope="axis2"/>

    <!-- TODO: Add your processing logic here -->

    <!-- Step 3: Log completion -->
    <log level="custom" category="INFO">
        <property name="ACTION" value="[{{NAME}}] Sequence completed"/>
    </log>

</sequence>
