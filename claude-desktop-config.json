{
  "_instructions": [
    "1. npm install && npm run build",
    "2. Copy the 'kafka-mi' block into your MCP client config",
    "3. Replace /ABSOLUTE/PATH/TO with the real path to this project",
    "4. Restart your AI client",
    "",
    "Works with: Claude Desktop, Claude Code, Cursor, Windsurf, Codex, Zed, Continue.dev",
    "See README.md Step 4 for client-specific config locations"
  ],
  "mcpServers": {
    "kafka-mi": {
      "command": "node",
      "args": ["/ABSOLUTE/PATH/TO/wso2-mi-kafka-mcp-server/dist/index.js"]
    }
  }
}
